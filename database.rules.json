{
  "rules": {
    ".read": "auth != null",
    "users": {
      "$uid": {
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')"
      }
    },
    "crops": {
      "$cropId": {
        ".write": "auth != null && ((!data.exists() && newData.child('farmerId').val() === auth.uid) || (data.exists() && data.child('farmerId').val() === auth.uid))"
      }
    },
    "buyerRequests": {
      "$requestId": {
        ".write": "auth != null && ((!data.exists() && newData.child('buyerId').val() === auth.uid) || (data.exists() && data.child('buyerId').val() === auth.uid) || (data.exists() && newData.exists() && root.child('users').child(auth.uid).child('role').val() === 'farmer' && data.child('status').val() === 'open' && newData.child('status').val() === 'deal_sent' && newData.child('buyerId').val() === data.child('buyerId').val()))"
      }
    },
    "deals": {
      "$dealId": {
        ".write": "auth != null && ((!data.exists() && newData.exists() && (newData.child('buyerId').val() === auth.uid || newData.child('farmerId').val() === auth.uid)) || (data.exists() && newData.exists() && (data.child('buyerId').val() === auth.uid || data.child('farmerId').val() === auth.uid)))",
        ".validate": "!data.exists() || (newData.child('buyerId').val() === data.child('buyerId').val() && newData.child('farmerId').val() === data.child('farmerId').val())"
      }
    },
    "reviews": {
      ".read": true,
      "$reviewId": {
        ".write": "auth != null && !data.exists() && newData.child('buyerId').val() === auth.uid && root.child('deals').child(newData.child('dealId').val()).child('buyerId').val() === auth.uid && root.child('deals').child(newData.child('dealId').val()).child('farmerId').val() === newData.child('farmerId').val()"
      }
    }
  }
}
